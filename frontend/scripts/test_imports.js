import * as rw from 'react-window';
import * as rvas from 'react-virtualized-auto-sizer';

console.log('React Window keys:', Object.keys(rw));
console.log('Auto Sizer keys:', Object.keys(rvas));

import rwDefault from 'react-window';
import rvasDefault from 'react-virtualized-auto-sizer';

console.log('React Window Default:', rwDefault);
console.log('Auto Sizer Default:', rvasDefault);
